#include <iostream>
using namespace std;

void swap(int &x, int &y)
{
    int tmp = y;
    y = x;
    x = tmp;
}

int main() {
    int x, y;
    cin >> x >> y;
    swap(x, y);
    cout << x << " " << y;
}
